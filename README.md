# integration-e2e-autoscan-repo

Static JavaScript repository used by SonarQube Cloud GitHub Autoscan E2E coverage.

The E2E suite may create temporary pull-request branches here, but the repository itself is preserved between runs.
